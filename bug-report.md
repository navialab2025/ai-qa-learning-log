# AI挙動不具合・バグレポート (Bug Reports)

本ファイルでは、AI（LLM）アプリケーションの検証中に発見した不具合や、実務導入におけるリスクとなる挙動をプロのQAフォーマットで記録しています。

---

## [Bug-001] AI出力におけるJSONキー名の不一致（再現性・不安定性の問題）

### 📑 概要 (Description)
API（モデル：`gpt-4o-mini`）を組み込んだ自作のCLIテスト用アプリケーションにおいて、プロンプトで出力フォーマットを指定した際、返却されたJSON自体の構文は正しい（Valid JSON）ものの、**APIリクエストを投げるたびにキー名（Key Names）が動的に変化してしまう現象**が発生。

* **検証環境**: 自作CLIテストアプリケーション（API連携）
* **使用モデル**: `gpt-4o-mini`
* **重要度 (Severity)**：高 (High)
* **影響リスク (Risk)**：
  バックエンド（プログラム側）で特定のキー名（例: `product_name`）をパース（解析）して変数に格納する設計にしている場合、AI側が勝手にキー名を変える（例: `item_name`）ことで、プログラムがデータを読み込めず、KeyErrorによるアプリの強制終了（クラッシュ）を引き起こします。

### 再現手順 (Steps to Reproduce)
1. ターミナルから自作のCLIテストアプリを起動。
2. `gpt-4o-mini` のAPIに対し、以下のプロンプト（システムプロンプト/ユーザープロンプト）を送信する。
3. 同一のスクリプト、あるいは連続したAPIコールで複数回（3〜5回）実行し、ターミナルに出力されるJSONレスポンスを確認する。

### 実際の結果 (Actual Result)
JSONの構文としてはエラーが出ないものの、実行回数によってキー名が以下のように揺らぎました。

* **1回目の実行**: `{"item_name": "天然ブリ", "price": 1200, "origin": "石川県"}`
* **2回目の実行**: `{"product_name": "天然ブリ", "unit_price": 1200, "area": "石川県"}`
* **3回目の実行**: `{"name": "天然ブリ", "cost": 1200, "source": "石川県"}`

### 期待される結果 (Expected Result)
何度実行しても、指定したスキーマ（構造）通りに固定されたキー名で返却されること。
* **期待値**：`{"product_name": "...", "price": ..., "origin": "..."}`

### 対策案・QA視点からの提案 (Suggested Remediation)
1. **プロンプトでの厳格なスキーマ定義**：単に「JSONで」と指示するのではなく、`"キー名は必ず 'product_name', 'price', 'origin' としてください。これ以外のキーは一切認めません"` とフォーマットを厳密に縛る指示（Few-Shot例の追加など）をプロンプト側で強化する。
2. **System Prompt / API設定の変更**：OpenAI APIであれば `response_format: { type: "json_object" }` の活用や、JSON Schema（Structured Outputs）を強制する設定を開発側に提案する。

---

## [Bug-002] 同一プロンプト連続実行時における「JSON外テキスト（枕詞）」の出力揺らぎ

### 📑 概要 (Description)
`gpt-4o-mini` を用い、出力にJSON形式を指定した際、同一プロンプト・同一環境であるにもかかわらず、**「JSONデータのみを返す回」と「JSONの前に余計な説明文（前置き）を出力する回」がランダムに発生する現象**を確認。

* **検証環境**: 自作CLIテストアプリケーション（API連携）
* **使用モデル**: `gpt-4o-mini`
* **重要度 (Severity)**：高 (High)
* **影響リスク (Risk)**：
  プログラム側で `json.loads()` などを使い、返却された文字列をそのままJSONとしてパース（解析）する実装にしている場合、「以下はAIについての説明をJSON形式で表現したものです。」といったプログラムが解釈できない日本語テキストが先頭に含まれることで、パースエラー（JSONException）が発生し、システムが停止します。

### 再現手順 (Steps to Reproduce)
1. 以下のプロンプトを5回連続で送信し、レスポンスの構造を比較する。
   > プロンプト：「AIとは何ですか？ explain in JSON」

### 実際の結果 (Actual Result)
5回中、2回で「JSON外のテキスト」が出力に混入しました。

* **1回目・4回目のレスポンス (不具合)**:
  「以下はAIについての説明をJSON形式で表現したものです。 ```json { ... } ``` このJSONは〜」のように、テキストとコードブロックが混ざって返却された。
* **2回目・3回目・5回目のレスポンス (正常)**:
  ```json { ... } ``` のみ（純粋なJSON構造のみ）が返却された。

### 期待される結果 (Expected Result)
API連携の安定性を担保するため、余計な説明文は一切排除し、常にパース可能な純粋なJSON構造（またはコードブロックのみ）が返却されること。

### 対策案・QA視点からの提案
1. **プロンプトでの「前置き拒否」の厳格化**：
   `"Do not include any conversational text, explanations, or markdown blocks outside of the valid JSON structure."`（前置きや解説、外側のテキストは一切禁止）という指示をシステムプロンプトに徹底する。
2. **プログラム側のガードレール実装**：
   AIの出力をプログラム側で受け取る際、正規表現などを用いて ````json ` と ```` の間だけを抽出してからパースする処理（トリミング処理）を実装するよう、開発側にフィードバックする。

---

## [Bug-003] 同一プロンプト連続実行時における「JSONデータ構造（スキーマ）」の非固定化

### 📑 概要 (Description)
[Bug-001]で指摘したキー名の揺らぎに加え、**データの階層構造（配列型か、オブジェクト型か）自体が実行ごとに変化してしまう現象**を確認。

* **検証環境**: 自作CLIテストアプリケーション（API連携）
* **使用モデル**: `gpt-4o-mini`
* **重要度 (Severity)**：高 (High)
* **影響リスク (Risk)**：
  プログラム側が「`種類` の中はリスト（配列）で返ってくる」と想定してループ処理を組んでいた場合、AIが勝用にオブジェクト（連想配列）に構造を変えてしまうと、プログラム側で型エラー（TypeError）が発生し、システムがクラッシュします。

### 再現手順 (Steps to Reproduce)
1. 「AIとは何ですか？ explain in JSON」を複数回実行し、`種類`（またはそれに準ずる項目）のデータ型を確認する。

### 実際の結果 (Actual Result)
同じプロンプトであるにもかかわらず、AIが定義した「データ構造の型」が実行ごとに変化した。

* **3回目のレスポンス（配列型）**:
  `"種類": [ {"名称": "弱いAI", "説明": "..."}, {"名称": "強いAI", "説明": "..."} ]`
* **4回目のレスポンス（オブジェクト型）**:
  `"種類": { "弱いAI": "...", "strong_ai": "..." }`

### 期待される結果 (Expected Result)
データ構造（スキーマ）が常に一定であり、プログラム側で予測可能な型で返却されること。

### 対策案・QA視点からの提案
* プロンプト内で、オブジェクトのネスト（階層）や、どこを `[]（配列）` にし、どこを `{}` にするのかを具体的な「出力サンプル（JSON Schema）」として明示し、固定化する。

---

## [Test-Log-001] 短文・記号・絵文字連打によるAI対話耐久性テスト（正常系・異常系の境界値検証）

### 📑 概要 (Description)
ユーザーが意図しない操作（1文字送信、空白送信、記号のみ送信、絵文字のみ連打）を行った際の、アプリケーションおよびAIモデルの挙動の耐久性を検証。

* **検証環境**: 自作CLIテストアプリケーション（API連携）
* **検証ステータス**: **正常（アプリ側のバリデーションが良好に機能していることを確認）**

### 検証内容と結果一覧
1. **1文字入力 (`a`)**: AIボットはエラーを起こさず、「ご質問の内容が不明です」と適切にユーザーに再入力を促すメッセージを返却（正常）。
2. **記号のみ (`?`)**: AIボットはクラッシュせず、「ご質問が見当たりませんが」と丁寧に応答（正常）。
3. **空白入力 (スペースのみ/空送信)**：アプリ側で入力を検知し、APIを叩く前に **`⚠️ 入力が空です。質問を入力してください。`** というシステムエラーメッセージをターミナル（CLI）上に表示。無駄なAPIコストの消費を防いでいる（非常に良好な実装）。
4. **絵文字の連打 (`😀😀😀😀`)**: AIは絵文字に引っ張られてバグることなく、「こんにちは！何かお手伝いできることがあれば教えてください。」と文脈を維持した挨拶を返却（正常）。

### QAエンジニアからの総評
本CLIアプリケーションは、空文字送信に対するフロント側（CLI側）のバリデーション（入力チェック）が完璧に実装されており、**無駄なAPIトークン（コスト）を消費させない堅牢な設計**になっていることを確認しました。記号や絵文字に対してもAIが例外エラーを吐くことなく、制御された応答を返せています。
